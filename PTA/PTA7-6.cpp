#include<iostream>
#include<vector>
using namespace std;

int main()
{
    int L,n;
    cin>>L;
    cin>>n;
    vector<int>v1(n);
    vector<int>v2(n);
    for(int i=0;i<n;i++)
    {
        cin>>v1[i];
    }
    for(int i=0;i<n;i++)
    {
        v2[i]=-v1[i];
    }
    for(int i=0;i<n;i++)
    {
        for(int )
    }
    system("pause");
    return 0;
}