#include<iostream>
#include<vector>
using namespace std;
void Judge(vector<int> vec1,int k)
{
    for (int i = 0; i < vec1.size();i++)
    {
        int sum=0;
        sum += vec1[i];
    }
}
int main()
{
    //输入
    int n,k;
    cin >> n;
    vector<int> vec1;
    for (int i = 0; i < n;i++)
    {
        int temp;
        cin >> temp;
        vec1.push_back(temp);
    }
    cin >> k;

    //判断

    system("pause");
    return 0;
}